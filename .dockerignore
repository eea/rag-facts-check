# Version control
.git/
.github/

# Virtual environments
.venv/
venv/

# IDE
.vscode/
.idea/

# Testing
.pytest_cache/
.coverage
htmlcov/
# tests/ is kept in the build context for the Dockerfile `test` stage.
# The `runtime` stage does not COPY it, so the production image is unaffected.

# Development
.env
.env.local
AGENTS.md
Makefile
pytest.ini

# Generated
__pycache__/
*.pyc
*.egg-info/
dist/
build/

# Documentation (not needed at runtime)
docs/
README.md

# Mock datasets
# mock_datasets/ is kept in context for the Dockerfile `test` stage;
# the `runtime` stage does not COPY it.

# Scripts
# scripts/ is kept in context for the Dockerfile `test` stage;
# the `runtime` stage does not COPY it.

# Agent skills
.agents/

# Example files
example_usage.py
