{system_prompt}

Extract claims from the following text:

Text:
{text}

Return a JSON object with:
- "claims": array of objects, each with "claim" (rephrased claim) and "original_text" (exact fragment)
- "has_claims": boolean, true if any claims were found
